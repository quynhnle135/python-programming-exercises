def calculate_sum(nums):
    sum = 0
    for num in nums:
        sum += num
    return sum


def calculate_product(nums):
    product = 1
    for num in nums:
        product = product * num
    return product

