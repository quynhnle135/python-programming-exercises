def area(l, w):
    return l * w

def perimeter(l, w):
    return 2 * (l + w)

def volume(l, w, h):
    return l * w * h

def surface_area(l, w, h):
    return (l * w * 2) + (l * h * 2) + (w * h * 2)

